<h1>ユーザーのTweet一覧</h1>
<table class="table table-striped table-bordered table-hover table-condensed">
  <thead>
    <tr>
      <th>User</th><th>Message</th>
    </tr>
  </thead>
  <tbody>
      <% @comments.each do |comment|%>
        <%= "<tr><td>#{comment.name}</td><td>#{comment.message}</td></tr>" %>
      <% end %>
  </tbody>
</table>
